apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: emart-db
  namespace: emart
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets
    kind: ClusterSecretStore
  target:
    name: emart-db-credentials-dev-app-1
    creationPolicy: Owner
  data:
  - secretKey: DB_USERNAME
    remoteRef:
      key: emart-db-credentials-dev-app-1
      property: username

  - secretKey: DB_PASSWORD
    remoteRef:
      key: emart-db-credentials-dev-app-1
      property: password

  - secretKey: DB_HOST
    remoteRef:
      key: emart-db-credentials-dev-app-1
      property: db_endpoint

  - secretKey: DB_NAME
    remoteRef:
      key: emart-db-credentials-dev-app-1
      property: db_name